{
  "name": "gymmate",
  "private": true,
  "version": "3.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "TZ=America/Lima vitest",
    "test:ui": "TZ=America/Lima vitest --ui",
    "test:coverage": "TZ=America/Lima vitest run --coverage",
    "verificar:tokens": "node scripts/verificar-tokens.mjs",
    "verificar:runtime": "npm run build && TZ=America/Lima node scripts/verificar-runtime.mjs",
    "verificar": "npm run verificar:tokens && tsc --noEmit && TZ=America/Lima vitest run && npm run build && TZ=America/Lima node scripts/verificar-runtime.mjs && TZ=America/Lima node scripts/verificar-fidelidad.mjs && TZ=America/Lima node scripts/verificar-comportamiento.mjs",
    "verificar:fidelidad": "npm run build && TZ=America/Lima node scripts/verificar-fidelidad.mjs",
    "verificar:comportamiento": "npm run build && TZ=America/Lima node scripts/verificar-comportamiento.mjs"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "autoprefixer": "^10.4.16",
    "jsdom": "^27.3.0",
    "playwright": "^1.62.1",
    "postcss": "^8.4.32",
    "sharp": "^0.34.5",
    "typescript": "^5.3.0",
    "vite": "^5.0.0",
    "vite-plugin-pwa": "^0.17.0",
    "vitest": "^1.0.0",
    "workbox-window": "^7.0.0"
  }
}
